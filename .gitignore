node_modules/
.next/
out/
.env
.env.local
*.log
.DS_Store
next-env.d.ts

# 런타임에 업로드된 업무일지 데이터 (커밋 안 함)
data/logs.json

# 손님 장부 (개인정보 — 커밋 안 함)
data/customers.json
data/guestbook.json
data/.probe

# 프로젝트 보드 (로컬 개발용 저장본 — 커밋 안 함)
data/projects.json

# 오늘 한 일 메모 (로컬 개발용 저장본 — 커밋 안 함)
data/worklog.json

# 달력 일정 (로컬 개발용 저장본 — 커밋 안 함)
data/calendar.json

# 쪽지함 (로컬 개발용 저장본 — 커밋 안 함)
data/notes.json
